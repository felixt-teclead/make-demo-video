# Quirk record: fixture test page

One record per quirk (D-31: id, views, symptom, detection, mitigation). Empty in S1.

The page's planted defects (whiteout, black, orphan, nesteddim, skeleton, sticky, dash, halfpaint, jitter, pointer,
banner, late) are off by default and switched on per URL query; they are test inputs, not quirks of the clean page, so
they get no record here. Their list and the checks they serve: `env/www/fixture/README.md`.
